int returnToBoundaryCount(int* nums, int numsSize) {
   int x=0;
   int count=0;
   for(int i=0;i<numsSize;i++){
        x=x+nums[i];
           
           
    if(x==0)
    count++;}
    return count;
}
